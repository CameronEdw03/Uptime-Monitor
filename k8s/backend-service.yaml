apiVersion: v1
kind: Service
metadata:
  name: uptime-monitor-backend
  namespace: uptime-monitor
spec:
  selector:
    app: uptime-monitor-backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP